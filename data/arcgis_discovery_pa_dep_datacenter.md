# ArcGIS layer discovery: pa_dep_datacenter

Not resolved. Either no candidate cleared the score threshold or the top two tied. Ranked candidates below; pin the right one into the source config's "url" by hand.

| score | layer | via |
|---|---|---|
