# Roadmap Phase Status

Standing document. Update at the end of any session that changes a phase's position; keep the history log at the bottom append-only. Numbers below are as of the stated date and reflect the post-scraper simulated state (they become the live repo state after the next nightly run applies the overlay).

Last updated: 2026-09-02. Numbers below are read from the current data/ outputs, not from a simulated overlay. Frame: 337 project entities from 338 proposal rows, 153 decided, 87 decided+opposed (26 blocked). Landmark frame under the announcement anchor: 173 opposed projects anchored, 29 decided-and-dated, 96 pending scoreable once the gate opens; 48 decision dates outstanding. The opposed and decided+opposed counts fell on 2026-09-02 when the duplicate project_id repair removed phantom opposition that a merged id had attributed to five projects; the outcome model moved with them to n=87. County frame after the 2026-08-21 label polarity repair and the 2026-08-24 retrain: 327 enacted-restriction counties of 3,144 scored, base rate 10.4 pct, median AUC 0.808, Brier 0.080 against 0.093 no-skill, post-recalibration slope 0.995, on the seven-variable specification the re-registered selection rule chose on 2026-08-26. County coverage after the automated gap-closure loop: 87 census counties in scope (counties, not instrument episodes), national recall 0.989, 1 missing, 3 label false negatives. The 2026-08-05 figures (recall 0.696, 24 missing, 26 open worklist rows) are fully resolved.

## The five phases

| Phase | What it is | Status | Gate to next step |
|---|---|---|---|
| 1. Entity resolution + lifecycle dates | Project entities, event links, announced/decision dates | Infrastructure COMPLETE; data quality OPEN, permanently | Decision dates: 46 missing (data/decision_date_worklist.csv), 31 verified. Announced dates: 15 opposed projects unanchorable. Pre-decision event dating: sparse (median first-opp-to-decision gap near zero among dated projects) |
| 2. Control group / baseline universe | Matched unopposed comparables; extended by the verified-negative audit at project level and now by the coverage audit at county level | Matching COMPLETE; project audit batch 1 coded (9 purposive, 0 random), analysis layer (emergence_analysis.py) BUILT and WIRED to CI, emergence gate LOCKED; county coverage audit (coverage_audit.py) BUILT, repaired, wired, national recall 0.989; national ingest worklist CLEARED to 3 rows and the gap-closure loop (census refresh, gap report, candidate builder, gated promotion) fully automated | Project arm: random-stratum coverage (0 pct so far), binding constraint is the undeterminable share, four-criterion unlock gate registered 2026-07-27. County arm: 1 census-enacted county with no tracker record (Cherokee NC, held in the promotion exception queue for lack of a dated source), 3 label false negatives |
| 3. Outcome + survival models | Retrospective P(blocked) and time-to-decision on decided+opposed | Retrospective COMPLETE (AUC 0.80 [0.69-0.95], n=83); landmark anchor RE-REGISTERED to announced_date 2026-07-28, retrain GATE CLOSED for decision-date coverage, now re-evaluated weekly by gate-check.yml instead of only on a manual run | Landmark floors (n>=40, blocked>=12 per window). Under the announcement anchor the ceiling clears all floors at W=30 once dates are recovered; binding arm is the advanced arm, not blocked. Selection criterion still pre-registered 2026-07-23 |
| 4. Cost translation layer | Documented conversion of predicted observables to dollar ranges with published anchors | BUILT and running weekly in retrain.yml (data/cost_anchors.csv, data/cost_translation_methodology.md, data/cost_translation_demo.csv); NOT client-facing, deliberately | A promotable model to translate, and MW coverage: cost_translation.py never imputes capacity, so the 2026-09-10 scraper regression that took capacity_mw from 123 rows to 3 currently empties the demo. Publishing on contaminated inputs would violate defensibility |
| 5. Continuous retraining + calibration gate | calibration_gate.py + retrain.yml; promotion only on holding calibration | BUILT, waiting | A model that passes the gate; both landmark and emergence models are written to promote only through it |

Parallel track, outside the numbered phases: the county product line (county_aggregator.py, county_policy_model.py, restriction-model.html), live and framed as resemblance, never as project risk. Client-presentable again as of 2026-08-05. CI retrained the county model on the corrected labels and restriction-model.html reads every headline number live from county_policy_metrics.json, so the retrain propagated on its own. Two page defects were fixed on 2026-08-05: the choropleth saturation ceiling was a frozen 0.35 tuned to the old 6.1 pct base rate, which pinned 200 counties and 61 pct of the 327 positives at maximum colour and destroyed separation inside the high band, and is now derived as the 99th percentile of the current scores with 0.35 as a floor; and the page's fetch chains put the relative path first, which fails silently inside a Notion or Simple.ink embed, and now put raw.githubusercontent.com first per the standing rule. The two-mode clickable map (opposition-map.html) shipped 2026-07-28: county polygons carry history, context, and calibrated enacted-restriction scores; project pins carry case detail and outcome, with landmark risk bands appearing automatically once that gate opens. It reuses the boundary crosswalk and 0,0-coordinate guard from the existing pages.

## Where this leaves the client-usable score

The path is unchanged: landmark retrain gives P(blocked) risk bands for pending projects; the negative audit unlocks P(opposition emerges); the cost layer translates; the two-mode map delivers. The map delivery is now built and waiting on the models rather than the other way around. What changed in 2026-07 is the understanding of the bottleneck: it is not modeling and it is not UI, it is Phase 1/2 data quality. The phases went recursive, and that is by design of the defensibility principle, not a stall.

## Known data-quality ledger (open items)

- prj_61: Clark County ARKANSAS miscoding, CONFIRMED on the state field 2026-09-02 by the new geographic gate: the row reads state "Kentucky" while its address is "Southwest Arkansas Mega Site" and its coordinates (34.054, -93.106) are in Clark County, Arkansas, a county name both states carry. The outcome coding is still suspect and unverified. It is a CMS-owned row (id below the manual-addition floor), so it has to be corrected at the source rather than in-repo; it is exempted in control_group.KNOWN_BAD until then. NOT a duplicates-file entry.
- prj_232, prj_266: mid-litigation, blocked_confirmed coding under defensibility review; no decision dates supplied.
- prj_101: opposition event dated 2025-10-01 predates public knowledge (late-Dec 2025 legal ad); fix rotates the opp_id, re-key manual rows in the same pass.
- Seven batch-1 verified_opposition projects (prj_295, 279, 201, 331 via 76, 86, 16, 104) need opposition events collected into master_opposition; each coding row carries sources. The audit analysis layer now emits these to data/audit_discovered_opposition.csv with the evidence URLs.
- Asymmetry worklist: prj_101 (above), prj_158 (genuine pre-application opposition, not an error), prj_297 (month-floor artifact).
- County coverage: 1 census-enacted county carries zero tracker restrictive record (Cherokee NC), down from 24 on 2026-08-05 and 30 on 2026-07-30. It is not an ingest failure: the census row carries no usable date, and a dateless row is held out of the clean feed as incomplete, so the promotion loop parks it in the exception queue (data/gap_promotion_report.csv, action held) until a dated primary source exists. National recall_any is 0.989. recall_confirmed is the honest remaining gap at 0.448 (39 of 87): most covered counties carry a tracker record whose terminal confirmation is still unverified, which is a confirmation-depth problem, not a coverage one, and is what the stale-pending queue works.
- County label reconciliation: 3 label false negatives remain, all of them true reconciliation rather than collection: Indiana DeKalb (tracker pending versus census active), Pennsylvania Montour (conditional_zoning typing needs confirming), and North Carolina Cherokee (the undated row above). The 20 collection-fixable false negatives of 2026-08-05 are closed. All three are priority 1 in data/national_restriction_worklist.csv, which is now 3 rows against 26 on 2026-08-05.
- County label divergence, other direction: CLOSED 2026-08-21. The four counties (Colorado Larimer, Georgia Pike, North Dakota Mercer, Nebraska Hitchcock) were symptoms of the label polarity defect, not of two rules disagreeing: county_aggregator.py counted Status "approved" as enacted regardless of what was approved, so a project approval over a sought-but-unenacted restriction labeled the county restricted. The direction guard added 2026-08-21 reads the raw Community Outcome field for direction; 28 false-positive counties were removed, 9 genuine gap counties added, Spotsylvania VA re-recorded correctly, and Powhatan VA now reads not-enacted. label_positive_no_tracker_record is 0 in every state.
- Indiana restriction worklist: CLOSED as a standing worklist. Indiana now shows 15 census counties in scope, recall_any 1.0, recall_confirmed 0.533, and one label false negative (DeKalb, above). The state-specific file is gone; Indiana rows flow through the national worklist like every other state.
- docs/indiana_fact_pack.md is on main and Facts 5, 10, and 11 are VOID along with every comparison derived from them; the county count was wrong by roughly a factor of three when written. Record-level and project-level facts in that document are unaffected. Nothing Indiana-facing ships until the worklist above resolves.
- Landmark date worklists (both emitted by the re-anchored landmark_model.py): data/decision_date_worklist.csv holds 46 decided projects missing a verified decision date and is the primary gate opener; data/announce_date_worklist.csv holds 15 opposed projects with no announced_date, which cannot be anchored at all until one is sourced. data/landmark_recovery_priority.csv ranks the 46 by whether recovery actually moves a frame at W=30. gate-check.yml keeps all three current weekly, which is the staleness it was added to prevent.

- Negative audit coding orphan: one batch-1 coding row is keyed to prj_76, which the duplicate suppression retired in favour of prj_1011. negative_audit.py reports it as a validation problem and ignores the row. Re-key it to prj_1011 in data/negative_audit_codings.csv.
- Headline metric unit defect (fixed 2026-08-05): headline_metrics.md labeled its decided-case block rate "Project level" while the denominator was primary incident RECORDS. 610 records carry a terminal outcome_defensible against 148 decided project entities, so the same claim read four times larger depending on which number was quoted. metrics.py now labels the record rate as a record rate and reports the project-registry rate beside it (24 pct of 148 decided projects, 36 blocked_confirmed).
- LF line-ending rule: 35 of 64 CSVs in the repo are CRLF. 24 modules call csv.writer or DictWriter without lineterminator="\n", and newline="" sends the default CRLF straight to disk. master_opposition.csv is among them; flipping the source of truth is a whole-file diff and has not been done.
- Selftest coverage: 19 of 53 modules. The gap that matters most is qc/legislative_outcome.py, which governs the approved-is-not-law principle and has no test at all; then county_aggregator.py, county_policy_model.py, project_resolution.py, metrics.py, group_registry.py.
- Stale-pending queue (data/stale_pending_worklist.csv, 233 stale rows of 267 scoped): 14 priority-1 rows are label moving, meaning confirmation of the pending measure would change a county's enacted label. Each is one search asking whether the measure passed, died, or lapsed. Age is a prompt to re-verify, never evidence that a status changed.
- Frame registry: configs/frames/tva_198_counties.csv does not exist, so the TVA subframe audit stays PROVISIONAL. Supplying the county list from the deliverable's appendix flips it to COMPLETE on the next nightly run with no code change.
- PA DEP layer URL: discovery (discover_arcgis_layer.py) ran in CI and returned zero candidates, so the layer cannot be resolved programmatically. It needs a one-time manual pin from browser dev tools before configs/pa_dep_datacenter_ingest.json can be written and join the weekly permit run. See data/arcgis_discovery_pa_dep_datacenter.md.
- Facility geocodes contradicting their own addresses (surfaced 2026-09-02 by the state-bounds gate, five rows in ai_centers.csv): aic_0003 Meta Prometheus (New Albany OH address, plotted in Smyrna GA), aic_0018 Vantage TX1 (Goodyear AZ address, plotted in Fort Worth TX), aic_0020 Meta Hyperion (Holly Ridge LA address, plotted in Beaverton OR), aic_0021 Goodnight (Claude TX address, plotted in Raleigh NC), aic_0024 Coreweave Helios (Afton TX address, plotted in Indianapolis IN). Three are exact city-centre coordinates, the signature of a geocoder falling back to a city when it cannot place the street address. The addresses are the record; the coordinates are wrong. Fix by geocoding each address with a citation, or by nulling the coordinates pending one -- not by moving the pin somewhere plausible. All five are exempted in control_group.KNOWN_BAD, which is a debt list that should shrink to nothing.
- Facility layer acquisition: the registry, identifiers and provenance shipped 2026-08-26; acquisition did not, and cannot from CI. Four sources are blocked on one manual look each because the egress proxy blocks their hosts: the OSTI IM3 atlas release URL and cadence, the Epoch export path, four hyperscaler footprint pages, and the PA DEP layer URL. Each is declared in configs/facility_sources.json with its mechanism, its rule and what it waits on, and the Data Operations page shows the list.

## Update log (append-only)

- 2026-07-23: Announced-date recovery (6 projects, overlay rows). Landmark module built, criterion pre-registered, gate reported CLOSED; 4 blocked-arm decision dates recovered. Negative audit designed (census of 176, now 175) and batch 1 executed: 7 of 10 blocked-without-recorded-opposition rows were detection gaps, 1 true negative (Millinocket, commercial failure), 2 undeterminable. Coding corrections: prj_141 voided approval and prj_1002 active-not-withdrawn (overlay), prj_76/331 duplicate (new suppression mechanism in project_resolution). Frame moved n=85 to n=83 (25 blocked); both removals are corrections, not attrition.
- 2026-07-28: Cleared priority items 1 through 5 from the 2026-07-27 handoff, plus the two medium-term modeling gates and the map. Splink spike (splink_spike.py) run against live data: NO-GO on adoption, AUC 0.63 on adjudicated pairs and 0.61 on the contested Type B cases where structured fields agree by construction; kept as a manual disagreement-audit tool only. Open States sync (bill_sync.py + bill-sync.yml) built stdlib-only, stage classification off machine-coded action histories, HF2690 milestone-as-enacted trap flagged HIGH; needs OPENSTATES_API_KEY secret to resolve. PUDL fetch (fetch_pudl.py + fetch-pudl.yml) built with runtime schema resolution against release drift; not yet joined into county_aggregator. Interconnection queue SCOPED (docs/interconnection_queue_scoping.md): reversed the Pass 1 premise, LBNL Queued Up is generation-only and excludes load, so it cannot be a data center baseline universe; take it for cost anchors only, revisit the paid tier Q4 2026. Verified-negative audit analysis layer (emergence_analysis.py): Manski bounds on the emergence rate, purposive and random strata separated, emergence gate LOCKED at 0% random coverage; key finding is that the binding constraint is the undeterminable share (bound width equals it at full coverage), not sample size, which argues for moving civic-scraper up. Landmark diagnosed and RE-ANCHORED: the opposition anchor was infeasible (majority of decided projects have first opposition dated at or after decision, a detection property, not a coverage gap), so t0 re-registered to announced_date 2026-07-28; frames now populate (W=30: 24 with 18 blocked vs the old 7/5), gate still CLOSED for decision-date coverage but the ceiling clears all floors once recovered. days_to_first_opposition right-censored at the window boundary to prevent post-window leakage under the new anchor. Two-mode clickable map (opposition-map.html) shipped. PHASE_STATUS refreshed.
- 2026-07-30: Entity-layer and vocabulary-enforcement pass, plus county label repair and the first recall measurement. Entry point was an Indiana entity verification; every defect found was national and every fix shipped is state-agnostic. (a) entity_split.py replaces three divergent splitters for the free-text multi-entity columns; the outcome and landmark models were splitting company names on [;,/] with no bracket awareness, so cells like "Multiple (QTS, Switch, CyrusOne, and others)" produced the model feature tokens "multiple (qts" and "and others)". 73 phantom group tokens and 52 phantom company tokens no longer emitted, feature schema unchanged. (b) group_registry.py fuzzy merge guarded: norm_key strips organizational-form words, so Indiana's Citizens Action Coalition scored 0.909 against WV Citizens Action Group and merged; a match must now also hold with the form word retained. Structural defects 86 to 17, split artifacts 69 to 1. group_registry_audit.py added as the standing check. (c) leak_audit.py generalized from six private per-module copies to a repo-wide tiered scan and wired into pipeline.yml as a hard gate; the previous arrangement audited generated prose only, which is how a stale docstring carrying retired vocabulary survived for the life of a module. (d) county_aggregator.py label rule repaired: has_enacted_restrictive used exact string equality on the multi-valued Opposition Type field and a two-value status vocabulary, catching 78 of 125 of the tracker's own confirmed county-level halts (Bulloch County GA has a confirmed enacted ban in the tracker and trained as a negative). Positives 197 to 327, none dropped, base rate 6.1 to 10.4 pct. CI retrained the county model on the corrected labels; restriction-model.html copy is now stale and the page is off-limits to clients until rewritten. (e) coverage_audit.py built: nothing had ever measured the county layer's recall, which is why the tracker could not know it was short. External census seeded from the Moratorium Nation dataset (mjbommar/moratorium-data-2026, CC-BY-4.0, 222 moratoria coded from roughly 4,400 primary documents) into data/external_restriction_census.csv, now a standing external dependency with refresh cadence undecided. First run: 80 census counties in scope, national recall 0.625, 30 missing, 21 label false negatives. Three rulings recorded: internal-only artifacts may quote raw scorekeeping vocabulary where the quoted value is what the reviewer needs (implemented as exact file-plus-column pairs in leak_audit.py, which took the blocking tier to zero and made the CI gate possible); the group-level outcome columns (decided, confirmed_blocks, blocked_share) are permanently internal at any sample size because they attribute project outcomes to named organizations from observational data with no control group, the same non-identifiability wall already ruled for cost and delay, with county-level and project-level four-tier statistics explicitly still client-eligible; and, standing across the platform, changes to headline model scores are acceptable and never a reason to block or revert provided the change improves precision and coverage of inputs and compartmentalization of the algorithms, with input integrity and architectural separation ranking above metric stability and the calibration gate still governing promotion.

- 2026-08-05: National ingest worklist, coverage audit repair, CI wiring, and three client-facing corrections. (a) restriction_worklist.py built: converts the coverage gap report into 26 ranked, actionable rows with a pre-registered ordering criterion (reconciliation before collection, label-moving before label-agreeing), a master_opposition-shaped ingest template with the census status mapped into the vocabulary the county label rule counts, and merge-preserving regeneration so a CI auto-commit cannot discard a reviewer's sourcing. Nothing is ingestable until a primary source URL is supplied; the external census is a pointer, not a source of record. (b) coverage_audit.py repaired on three defects found by building the worklist against it: norm_county did not strip parentheticals or governing-body suffixes, so seven census rows never joined the national frame, and because the false-negative test was label == "0", an unjoined row could never register as label noise, which silenced the exact signal the module exists to raise; the unit of analysis was the census episode rather than the county, so North Dakota's Oliver County counted three times and inflated both the national denominator and that state's missing count; and the reverse-direction disagreement was invisible. Recall moved 0.625 to 0.696, missing 30 to 24, label false negatives 21 to 22, unjoined 7 to 0. (c) pipeline.yml: coverage_audit.py moved to run AFTER the county layer rather than before it, where it had been measuring the previous run's labels and would have reported a repaired label as still broken; audit artifact paths were absent from the commit glob entirely, which is why group_registry_audit.csv, coverage_gap_report.csv and the summaries ran in CI but never appeared in the repo. (d) gate-check.yml added: landmark_model.py, negative_audit.py and emergence_analysis.py were in no workflow and imported by nothing, so both closed gates could only ever open if someone remembered to run a module by hand, the date worklists went stale the moment a date was recovered, and opposition-map.html's risk bands were unreachable by construction because nothing in CI could produce landmark_model_predictions.csv. Weekly plus push on gate-opening inputs; promotion discipline unchanged, since landmark writes model artifacts only on an admissible window. (e) restriction-model.html: choropleth saturation ceiling derived from the current score distribution instead of a constant frozen at the old base rate, and fetch chains reordered raw-first. (f) opposition-tracker.html and opposition-dashboard.html: fetch chains reordered raw-first; zero relative-first chains remain anywhere in the repo. (g) metrics.py: the headline decided-case rate was labeled project level while counting incident records, now labeled correctly with the project-registry rate reported beside it and the reason the two differ stated in the artifact itself.

- 2026-08-25: Standing-state refresh after the 2026-08-21 to 2026-08-25 block, recorded here from the pass plan of the same date. (a) Label polarity closed: county_aggregator.py counted Status "approved" as enacted without asking what was approved, so counties where a project was approved over a sought-but-unenacted restriction trained as restricted. The direction guard reads the raw Community Outcome field, which carried the direction in every case but one. 28 false-positive counties removed, 9 genuine gap counties added, Spotsylvania VA re-recorded, Powhatan VA now not-enacted, positives 327 to 326, and the four label_positive_no_tracker_record divergences of 2026-08-05 resolved as symptoms of the same defect rather than as two rules disagreeing. (b) The gap-closure loop is automated end to end: monthly census refresh, coverage audit, ranked worklist, candidate builder, and gated promotion, with every promote/hold/block decision written to data/gap_promotion_report.csv. National recall_any moved 0.696 to 0.989, missing 24 to 1, open worklist rows 26 to 3. The one open county (Cherokee NC) is held rather than missed: its census row carries no usable date and a dateless row is incomplete for the clean feed, so it promotes itself the moment a dated primary source enters the census. recall_confirmed at 0.448 is now the honest remaining gap and is a confirmation-depth problem worked by the stale-pending queue, not a collection problem. (c) The daily upstream sync carries the ownership rule and status normalization that ended the master_opposition corruption; the QC gate holds at zero findings; all four auto-committing workflows carry rebase-retry push hardening. (d) Retrain gate passed 2026-08-24 (ECE 0.073, Brier skill 0.207, PROMOTE), the fifth consecutive PROMOTE. (e) Frame audits and permit acquisition are registry-driven; PA DEP discovery ran and did not resolve, which is a pinning task, not a build task. (f) Recorded, not fixed, and now leading the next block: the Notion hub exposes five datasets and two static facility snapshots while data/ holds well over a hundred generated outputs, so almost everything the pipeline knows is invisible; and the facility layer, the largest dataset here, has no acquisition pipeline at all.

- 2026-08-26: The 2026-08-25 pass plan executed, passes 0 through 3. Working backwards from the Notion surface, then barriers, then facility intake, in that order. (a) Pass 0, this document refreshed to the 2026-08-25 state. (b) Pass 1, the surface-versus-data gap measured for the first time and made a maintained artifact: visibility_audit.py walks the registered surfaces, parses their fetch chains, inventories every generated output and classifies each against a declared disposition, so invisibility has to be a decision. First run found 10 of 140 outputs reachable by a client. Verifying the hub against Notion corrected the assumed map: Infrastructure has two child pages carrying four embeds, and index.html is on no page at all. Four closures shipped. Facility snapshots now state their own provenance (configs/facility_sources.json, facility_manifest.py, facility-freshness.js), including the honest statement that neither upstream vintage is recorded, so the age of the data is unknown; the counts typed into master_datacenter_map.html's heading are gone. The Venn-Abers intervals are surfaced on both score surfaces, since county_policy_intervals.py rules that external use quotes the interval and never the point alone and both surfaces were quoting the point alone; the LAC conformal sets stay invisible, being an internal diagnostic that labels 98 pct of counties none_supported at a 10 pct base rate. data-operations.html plus operations_summary.py turn the audit trails into something a reader can see: coverage by state, both promotion trails, the full calibration verdict history, the open queues and both modeling gates against their pre-registered floors. Project Lifecycles states its four denominators and what the outcome gate is waiting on. Five relative-first fetch chains fixed; a front-end syntax gate (scripts/check_inline_js.py) and the three existing .js self-tests, which were in no workflow, are now in the blocking gate. (c) Pass 2, the dataset barriers made explicit and enforced: ARCHITECTURE.md declares five layers with their files, writers, keys and sources of record; layer_audit.py enforces one writer per file and no undeclared layer crossing, resolving write targets by AST walk. 108 written files, zero undeclared findings, four declared exceptions each carrying its argument. Both known blurs closed: baseline_universe.csv redeclared Layer E with its four build inputs named, and signal_harvest.py now routes facility signals out of the opposition worklist into data/facility_candidates.csv, guarded so an opposition mechanism always wins and a broad opposition-language guard catches what the mechanism list cannot. (d) Pass 3, Layer A gets a registry: 1,506 rows across the two snapshots clustering to 665 distinct sites, stable facility_id at ten-metre coordinate precision after two coarser attempts silently merged campuses with their buildings, and a gate that holds most of what reaches it because a headline names an operator and a county but not a facility. Announcements are blocked outright as Layer B. The Layer B graduation path is live and mechanical, with six projects promotable today. Acquisition is declared rather than half-built: every source carries what it is blocked on. (e) Not done and not started: Pass 4 (verified-negative audit, random arm), Pass 5 (decision-date recovery), Pass 6 (frames, staleness, hygiene ledger). All three are research sessions requiring outbound search, which this environment's egress policy blocks.

- 2026-08-26 (later): County model specification selection re-registered, at Price's direction. The 2026-08-25 design froze a hand-written list of five variable subsets and required the shipped model to score within 0.01 AUC of the best point in that frozen list. That rule is self-defeating and it fired: a subset reached AUC 0.8074 while sign-unstable, lifting the tolerance floor above every sign-stable point, and the module refused to ship anything at all. The county layer stopped being committed on every run, on main and on the branch alike. Widening C_GRID was tested and did not recover it: the winning subset only stabilizes at C=0.001, where it scores 0.7930, still under the floor. What is registered now is the RULE, not the variable list. Candidates are generated on every run by backward elimination from the full variable pool and from each named anchor; at each step the single least trustworthy variable is dropped, where least trustworthy is defined purely on how evenly its coefficient straddles zero and never on accuracy, so the elimination path cannot be steered toward a better score. Admissible is unchanged (sign-stable on all 25 folds); selection among admissible is unchanged (highest AUC, most variables, strongest regularization). The accuracy given up for stability is now reported as stability_cost rather than used as a veto, because under an active search the gap is a measured property of the data rather than an artifact of a stale list; a gap beyond 0.05 remains a hard stop. First run under the new rule selects a seven-variable specification at AUC 0.8076 with stability_cost 0.0, meaning the most accurate specification tried is also fully sign-stable and nothing was given up. margin_2024 and median_hh_income left the model, each with its coefficient range recorded as the evidence (margin_2024 ranged -0.112 to +0.126 across folds); margin_shift left as part of the winning anchor. Every future change appends to data/county_model_spec_history.csv with the same evidence, and restriction-model.html renders that history on the Model, Method, and Scoring page, so a variable entering or leaving the model is a client-visible, explained event rather than a silent diff. A variable that leaves can rejoin: every run restarts from the full pool. county_policy_model.py gained its first self-test, 34 checks over the selection rule, which closes the highest-priority gap in the selftest-coverage ledger. Promotion discipline unchanged: calibration_gate.py still governs. Also fixed: layer_audit.py could not see a file written behind the "path or DEFAULT" override idiom, so the one-writer rule was not governing those paths; it resolves them now and the written-file count moved 105 to 110.

- 2026-08-26 (debug pass): Cleanliness sweep before the next block of work. Every self-test in the repository runs green (43 python modules, 4 front-end modules), and the leak, layer, visibility and front-end syntax audits all pass in strict mode. Four findings, all fixed. (a) Every gated promoter appended its decision trail on every run rather than on every change, so a nightly schedule that re-decided the same candidates the same way buried the events under repetitions of the non-events: the harvested-signal trail held 7,342 rows describing 1,947 decisions, the facility trail 40 rows describing 10, the census gap trail 3 rows describing 1. This mattered beyond tidiness because three of those counts had just been put on the Data Operations page as evidence the platform maintains itself, and the harvested-signal count overstated it by 3.8x, which the defensibility principle forbids. promotion_trail.py now holds the rule once (record a decision when it differs from the last one recorded for that candidate) as pure functions with no file access, so each trail keeps exactly one writer and the layer audit still attributes it correctly. The accumulated duplicates were collapsed with the same tested function: 7,342 to 1,947, 40 to 10, 3 to 1, with no genuine decision or decision change dropped. Honest mix is now 1,070 blocked and 877 promoted. (b) promote_signal_candidates.py carried a self-test asserting that appending the same report twice doubled the rows, which encoded the defect as the expected behaviour; it now asserts the exact split of changed versus unchanged verdicts. Keying the signal trail on url alone was also wrong, because one batch legitimately emits two verdicts for one url (promoting the first occurrence, marking the rest duplicate), so the two rows overwrote each other and both were re-recorded forever; the key is url plus title. (c) county_policy_report.md, which restriction-model.html renders on the Notion page, still described the retired frozen-grid criterion including an AUC tolerance the model no longer applies. The generated prose now states the registered rule, the stability cost, and where to find the specification history; AUC_TOLERANCE is retired. (d) README.md listed none of the six modules added this block and ARCHITECTURE.md covered none of the new Layer A artifacts or the decision-trail rule; both updated, so the next session is not working from a stale map. Header figures refreshed to the current model: 327 positives, median AUC 0.808.

- 2026-09-02: Duplicate project_id repair. data/proposals.csv is fed by two writers, the CMS export (contiguous ids from 1) and manual additions curated in-repo; a July 2026 manual batch restarted numbering at 321, inside the CMS space, and minted ids 321 through 326 twice. project_id is the join key for every Layer B artifact and nothing validated it, so the joins fanned out and six Pennsylvania projects inherited another project's coordinates, developer and opposition events. Nebius Butler Township (Schuylkill County, PA) published at 32.507, -91.647, which is Richland Parish, Louisiana, operated by "Meta", carrying Meta Hyperion's ten opposition events; two different projects appeared under prj_322 in the outcome model's training features. (a) The manual-addition block renumbered 321-332 to 1001-1012, which also heads off the certain recurrence, since the CMS mints 327 next and would have collided with Meta Project Everest. Hand-curated references migrated in project_links_manual.csv, project_decision_dates.csv, project_duplicates.csv, negative_audit_codings.csv and this file; dated session records under data/ keep the pre-migration ids and are translated through the table now in ARCHITECTURE.md. (b) project_resolution.assert_unique_project_ids() added: a duplicate or blank id now stops the pipeline naming both projects, instead of publishing a merged one. (c) The whole repair is scripted and idempotent at scripts/fix_project_id_collision.py, with --check, --dry-run and --regen. (d) Post-repair frame: 337 projects, 153 decided, 188 opposed (was 193), 87 decided and opposed (was 89), 31 with a verified decision date, 29 anchored and dated. Decision-date worklist 48 (was 46). Outcome model n=87, AUC 0.85 [0.65-0.93], Brier 0.167 against a 0.210 base. Landmark frames unchanged at W=30 n=24, 18 blocked. (e) The gate sentence on project-lifecycles.html cited a bare worklist count against denominators a reader could not reconcile it with, and now names its pool and states the detection asymmetry: blocked is 26 pct of decided projects but 24 of the 31 dated ones, and the worklist is 47 of 48 advanced, so not_blocked>=12 is the binding floor, not n>=40. (f) Found in passing, not caused by this and not fixed: ai_centers.csv carries two facility rows whose coordinates contradict their own addresses, aic_0003 Meta Prometheus (New Albany OH, plotted at Smyrna GA) and aic_0020 Meta Hyperion (Holly Ridge LA, plotted at Beaverton OR). Both are live on the map.

- 2026-09-15: County profile built out into the client surface it is presented as, on the two axes it was thinnest: comparison and the people layer. (a) county_benchmarks.py built. The page carried 40-odd figures per county and no frame to read any of them against; a client asking "is 5 opposition events a lot" had nowhere to look. It publishes median and quartile profiles for six national reference groups (all counties, the 329 with an enacted restriction, the 2893 without, data-center-present, data-center-absent, and the 663 with any tracked activity) plus one group per state, the mid-rank percentile of each county within them, and a ten-county structural peer set matched on standardised log population, log density, bachelor's share and 2024 margin; 3,115 of 3,222 counties carry all four features and are matchable, the rest are published unmatched rather than imputed to the centre of the distribution. Two defects were caught by its own self-test and fixed before the first build shipped: the nearest-neighbour search compared squared partial distances against a rooted cutoff, pruning candidates that were actually closer (now verified against an unpruned brute force on a randomised frame), and a published 0-100 "proximity index" scaled between the restrictive and non-restrictive medians ran past 400 for ordinary high-scoring counties because those two medians sit only 9.4 points apart; it was removed rather than clipped, in favour of the percentile within the restrictive group, which answers the same question without a fragile denominator. That 9.4-point separation is now published in the manifest and stated on the page, because it is the honest caveat on the whole section. (b) stakeholder_positions.py built. data/bill_sync_votes.csv held 32,024 roll calls that nothing had ever read; the registry could say who decides in a county and nothing about where they stand. It publishes 11,196 position rows over 2,302 people from four evidence classes, each carrying its source URL, and infers nothing from party, office title or district. The subject gate is the reason this is publishable: bill_sync's matching also returned a Colorado child-restraint bill, a Minnesota immigration bill and an Ohio budget, so a bill's title must carry an explicit data center or large-load term before its roll calls can be read as positions on this subject. 72 of 174 bills pass it and 21,477 roll calls are withheld, including real bills (Maryland HB 1532 at 9,024 votes is the largest); the QC report ranks what was withheld by how many votes a human confirmation would unlock, and confirmations go in data/bill_subject_overrides.csv, which the module reads and never writes to. Direction comes from an ordered rule list on the title, with the firing rule published on every row; the ordering is load-bearing and its self-test asserts it, since "sales and use tax exemption; repeal" classified as `enabling` until the repeal rule's match window was widened across the semicolons legislative titles use to separate clauses. Stances sit on three axes -- restriction, industry incentive, disclosure -- and are never netted into one score; the record label was also loosened from all-or-nothing after the first render put a 15-1 voting record in the same `mixed` bucket as an 8-7 one. (c) county-profile.html gained both sections, wired into the CSV export and the methodology panel; the reference file is small enough to load in the core wave so the comparison table is populated at first paint. (d) positions-dashboard.html shipped as the national view of the same record, filterable by state, evidence class, stance and bill direction, and linked from every page's nav. (e) CI: benchmarks run in pipeline.yml gated on the county layer, staged separately so a benchmark failure leaves the scores committed and a score failure never pairs new scores with old reference groups; positions get their own positions-refresh.yml triggered on each of their five inputs, with the gate self-test BLOCKING, since the failure it guards against is attributing a position to a named person who never took it. (f) configs/layers.json: the benchmark, position and previously-undeclared stakeholder files are declared; undeclared findings 22 to 8, all remaining ones pre-existing and outside this work.

- 2026-09-15 (second pass): County Profiles registered on the Notion hub, and the position layer's withheld queue worked down. (a) configs/surfaces.json now carries the County Profiles page URL, so the page is no longer reported as living on no Notion page and the 29 datasets it reads are classified rather than unclassified. (b) Eight of the bills the subject gate had withheld were confirmed against cited sources and promoted in data/bill_subject_overrides.csv: Maine LD 307 (the repository held the pre-amendment committee title, so the gate could not see that the enacted title is "An Act to Establish the Maine Data Center Coordination Council and Place a Temporary Limitation on Certain Data Centers"), Maryland HB 1532, West Virginia HB 2014, Pennsylvania HB 2496, Arizona HB 2756, Oregon HB 4084, North Carolina SB 730 and Indiana HB 1333. Position rows 11,196 to 27,940, people 2,302 to 2,945, withheld roll calls 21,477 to 4,733. Nebraska LB 1261 and Washington SB 5982 were researched and deliberately NOT promoted: both touch large loads, neither is a data center bill, and reading an eminent domain carve-out or a CETA technical amendment as a data center position is the inference the gate exists to prevent. (c) A `subject_scope` field was added to the override schema, because promoting these exposed a distinction the first build did not have. Half of them are vehicles: Maryland HB 1532 is an omnibus energy affordability act that happens to create a data center registry, Oregon HB 4084 an enterprise zone extension that happens to carve data centers out of it. Their roll calls are real and are published, but a vote on the vehicle is not a position on the provision and the roll call cannot separate them, so a `partial` bill publishes with no direction. The rule is enforced in both directions: an override may assert a direction only on a bill it also declares `primary`, and `partial` suppresses a direction from every source including the title rules. North Carolina SB 730 forced that second half -- its title is "Ratepayer Protection Act", so the ratepayer rule fired and would have given all 236 of its votes a stance, on a bill that also reshapes long-range power planning and carries fossil and nuclear provisions that drew their own opposition. 45 of 80 admitted bills now carry a direction. (d) Both pages mark a vehicle bill "part of a wider bill" and say why. Montgomery County MD is the worked example: 9,024 recorded acts on the page, 185 legislators listed, and not one of them assigned a stance.

- 2026-09-15 (third pass): the bill subject gate replaced by a typology, and made fully automatic. The binary "does the title say data center" gate withheld 21,477 of 32,024 roll calls, and working that queue down by hand -- eight bills, each researched and cited -- made it obvious the queue was the wrong artifact. bill_taxonomy.py now classifies every matched bill on two axes. REACH is how the law gets to data centers: 61 name them outright, 13 bind a load or size class they dominate (large load customers, high load factor customers, a megawatt threshold), 28 carry the provisions inside a wider bill, 3 are wrong-bill matches and 69 establish nothing. INSTRUMENT is what the law does, and direction is derived from it rather than judged separately, so the two can no longer disagree. Reach decides stance eligibility: only the first two categories let a vote count as a position, so the hand-set `subject_scope` of the previous pass is now derived. Position rows 27,940 to 32,365, people 2,945 to 3,316, withheld roll calls 4,733 to 308 -- every remaining withheld vote is a wrong-bill match rather than a coverage gap. The evidence is the opposition record that put each bill in the repository in the first place, admitted under two guards: it must name the exact bill (one Arizona record names HB 2756, HB 2795 and HB 2457 in one summary and the match fans out to all three; identifier matching is boundary-aware in both directions so "SB 26" does not match Colorado's "SB 26-102" and "HB 275" does not match "HB 2756"), and the fetched title's subject must be one a data center provision could ride in. That second guard found three wrong bills the hand pass had missed or mis-filed: Colorado SB 26 is a child restraint bill, Minnesota HF 16 an immigration bill, Texas SB 6 an abortion bill, all three matched to records that genuinely are about data centers. They are now reported as bill_sync defects to repair rather than buried with the honest misses. Two further defects surfaced and were fixed at source: bill_sync.py truncated titles at 160 characters, so West Virginia HB 4983's title ended at "certification as a high i", one word short of "impact data center" -- the cap is now 600 and a title at the cap is flagged so its silence is never read as evidence; and 59 bills had no title at all because the Open States lookup came back http_429 or not_found, which is a spent quota rather than a coverage judgement and now has its own re-fetch queue in the report. stakeholder_positions.py keeps no lexicon of its own any more; it reads the taxonomy, and its self-tests cover the translation rather than a second copy of the rules.

- 2026-09-15 (fourth pass): adopted the refresh_external_census.py repair from the sister session's PR #30 after checking its claims against the live upstream rather than taking them on trust. Every one held. The module shipped as a placeholder and still carried its own "TODO: adjust these field names" marker, and each of its four field reads was wrong against the real Moratorium Nation header in the same direction: it read `state` ("Alabama") so `state_abbrev` in the fallback position was never reached; it took `jurisdiction` with no `jurisdiction_type` filter, so 173 City, 71 Township, 47 Town and 16 Village rows entered a county-level census (counts confirmed exactly against the live file); it read `status`, a column upstream does not have, with a `census_status` fallback that does not exist either, so the status was empty on all 533 rows; and it read `date_enacted`, which is free text on 360 of 533 rows ("On or about 2026-06-25, per WBRC reporting"), rather than `date_enacted_iso`, which upstream populates on 469. With no sector filter, crypto-only and battery-storage moratoria entered a data center census. make_row_key then compared raw strings across those mismatches, so every upstream row read as new and the delta was unusable -- the real reason the module's docstring records that the delta "has no automated consumer". Repaired, the delta is 118 county-level data center rows across 30 states, ISO-dated, with CC-BY-4.0 attribution and a stable upstream id per row. NOT adopted from that PR: restriction_evidence.py and its ledger, which is a larger architectural addition belonging to that branch and currently grades 3,014 of 3,222 counties U for want of a probe layer. The promotion contract is unchanged and was deliberately not touched -- this module still never writes the census, because promoting an upstream row is a review decision. What changed for the county page is that the delta is reviewable for the first time, so county-profile.html now reads it and shows a pending row as a question rather than telling a client "coverage gap" about a county an external compiler has already documented. 66 of those rows sit on counties this repository records as having no enacted restriction, which independently reproduces the ~62 candidate false negatives PR #30 reports; on those counties the page says so in terms, since quoting such a county as clear is the error worth preventing. The hardcoded "covers 94 counties" line on that section is now read from the census actually loaded.

- 2026-09-23: County profile now states the evidence behind its restriction label rather than leaving it implied. The page asserts a label on all 3,222 counties, and on the 2,893 it records as having none it made that assertion by rendering nothing at all -- the "Enacted restriction record" section is skipped entirely for a county with no instrument on file. An empty space is not a neutral absence to a reader deciding where to site a facility; it reads as "checked, and clear". restriction_evidence.py already records what was actually consulted per county and grades it against a rubric registered before any county was graded, and nothing on the client surface read that file. county-profile.html now loads data/restriction_evidence.csv and data/restriction_evidence_conflicts.csv and renders an "Evidence behind this label" section that always paints, giving the grade, whether any source was consulted, when, which families and independence classes, the county's cross-check conflicts in client language, and how many counties share the grade. Both files ride the CORE wave, not the detail wave: the label is claimed in the first paint, so the grade has to arrive with the claim it qualifies rather than behind it, and at 30 KB gzipped the ledger is smaller than the reference profiles already in that wave. Three defects found and fixed while building it, each caught by rendering in Chromium across all seven grade-by-conflict-by-label combinations present in the data rather than by reading the code. (1) The section read has_enacted_restrictive off slice.county, which is the picker wrapper; the aggregate row is on .agg, so the field was undefined, compared unequal to '1', and reported every county in the country as clear -- the precise failure the section exists to prevent. (2) The generic `.kpi span` rule outranks `.flag`, flattening the grade badge into muted block text and destroying the colour that separates "checked" from "never looked at". (3) Grade D is reached two ways and the page conflated them: a county no source has been consulted for, and a county whose source WAS consulted and returned nothing that can support the label, because a restriction census asserts positives only and can never clear a county. The second is legible only in families_checked, and telling a client "nothing here has been looked at" about a county where a reviewed census actively reported a moratorium would have been false. grade_reading() now carries the distinction and the badge title reads "Nothing supports this" for both routes. No probe layer was built and none could be: every probe host in the registry (municode, amlegal, ecode360, legistar, granicus) answers 403 to CONNECT under the egress policy where these sessions run, confirmed again here against municode. Manufacturing probe results from this repository's own tracker would be circular and would read downstream as verification, so the honest U stands. Visibility audit reclassifies both files from unclassified to surfaced; unclassified drops 28 to 27.
